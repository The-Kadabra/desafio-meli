version: '3.9'

services:
  app:
    build: ./app
    ports:
      - "3000:3000"
    volumes:
      - ./app:/usr/src/app       
      - node_modules:/usr/src/app/node_modules 
    environment:
      NODE_ENV: development

volumes:
  node_modules:
